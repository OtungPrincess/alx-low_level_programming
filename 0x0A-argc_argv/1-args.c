#include <stdio.h>

/**
 * main - prints number of arguments
 * @argc: argument count
 * @argv: arrays of pointers to argument
 * Return: 0
 */

int main(int argc, char *argv[])
{
	argv = argv;
	printf("%d\n", argc - 1);
	return (0);
}
